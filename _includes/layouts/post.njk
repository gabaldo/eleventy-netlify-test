---
layout: layouts/base.njk
---

<h2>{{ title }}</h2>
<p>Criado em <time>{{ post.date | date('DD/MM/YYYY') }}</time></p>
<img src="{{ featuredImage | url }}" alt="{{ featuredImage }}"></img>

<p>{{ content | safe }}</p>
